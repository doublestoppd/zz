import type { ExtractionSettings } from "@zombie/game-core";

/** Rounds the survivors must hold the extraction zone after first reaching it together. */
export const DEFAULT_EXTRACTION: ExtractionSettings = {
  holdoutRounds: 1,
};
