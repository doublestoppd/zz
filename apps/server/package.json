{
  "name": "@zombie/server",
  "version": "0.1.0",
  "private": true,
  "description": "Authoritative match server: sessions, lobby, and WebSocket transport.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\" --external:bufferutil --external:utf-8-validate --outfile=dist/server.js"
  },
  "dependencies": {
    "@zombie/game-core": "workspace:*",
    "@zombie/game-data": "workspace:*",
    "@zombie/map-generation": "workspace:*",
    "@zombie/protocol": "workspace:*",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.28.2",
    "tsx": "^4.23.13"
  }
}
